MINION 3

**VARIABLES**

<%
cat(sprintf("DISCRETE l[%s,%s] {1..%s}", N, N, N))
%>

**SEARCH**
PRINT ALL

**CONSTRAINTS**

# Latin constraints

## Rows

<%
cat(unlist(lapply(1:N, function(i) alldiff(l[i, ]))), sep = "\n")
%>

## Columns

<%
cat(unlist(lapply(1:N, function(i) alldiff(l[, i]))), sep = "\n")
%>

**EOF**
